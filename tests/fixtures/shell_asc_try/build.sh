#!/bin/bash
# Empty-shell marker file: fill-op must NOT touch anything but
# op_kernel/<entry>.cpp, op_kernel/<entry>_tiling.h and op_host/<entry>.cpp.
echo "placeholder build script of the msopgen shell (kept byte-for-byte)"
